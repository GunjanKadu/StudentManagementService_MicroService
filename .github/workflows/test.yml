name: Node.js CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install Main dependencies
        run: npm install

      - name: Install Student Service dependencies
        run: |
          cd studentService
          npm install

      - name: Run Student Service tests
        run: |
          cd studentService
          npm test

      - name: Fail if test files are changed
        run: |
          if git diff --name-only origin/main...HEAD | grep '^tests/'; then
            echo "❌ You are not allowed to modify files in the tests/ directory."
            exit 1
          fi

      - name: Upload coverage report (optional)
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: services/auth-service/coverage
